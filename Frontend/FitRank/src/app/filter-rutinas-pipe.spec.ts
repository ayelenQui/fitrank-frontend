import { FilterRutinasPipe } from './filter-rutinas-pipe';

describe('FilterRutinasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRutinasPipe();
    expect(pipe).toBeTruthy();
  });
});
